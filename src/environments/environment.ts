// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig : {
    apiKey: 'AIzaSyAjY8TCOcToy4vDO83T_VnLvo0OzZTugJM',
    authDomain: 'business-task-project.firebaseapp.com',
    databaseURL: 'https://business-task-project.firebaseio.com',
    projectId: 'business-task-project',
    storageBucket: 'business-task-project.appot.com',
    messagingSenderId: '675133571670',
    appId: '1:675133571670:web:67b45d193e87a881edf1f7',
    measurementId: 'G-GK5R6THYR2'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
